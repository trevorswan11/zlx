class Greeter {
    let name: string;

    fn ctor(name: string) {
        this.name = name;
    }

    fn greet() {
        println("Hello, " + this.name);
    }
}

let g = new Greeter("Ziggy");
let greet = g.greet;
greet();
