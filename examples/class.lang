class Person {
    let name: string;
    let age: number;

    fn constructor(n: string, a: number) {
        this.name = n;
        this.age = a;
    }
}

let p = new Person("Alice", 42);
print(p.name);