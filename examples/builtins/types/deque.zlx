import deque;

let d = new deque();
d.push_head(2);
d.push_tail(3);
d.push_head(1);
d.push_tail(4);
println(d.str());

let h1 = d.peek_head();
let t1 = d.peek_tail();
let pop1 = d.pop_head();
let pop2 = d.pop_tail();

println(h1, t1, pop1, pop2);