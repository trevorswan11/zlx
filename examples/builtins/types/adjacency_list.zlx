import adjacency_list;

let g = new adjacency_list();
g.add_edge(1, 2);
g.add_edge(1, 3);
g.add_edge(2, 4);
println(g.str());

let n1 = g.get_neighbors(1);
let n2 = g.get_neighbors(2);
let has1 = g.contains(1);
let has9 = g.contains(9);
let count = g.size();

println(n1, n2, has1, has9, count);