import linked_list;
        
let l = new linked_list();
l.append("a");
l.append("b");
l.append("c");
println(l.str());

let first = l.get(0);
let last = l.pop_tail();
let size = l.size();
let is_empty = l.empty();

println(first, last, size, is_empty);